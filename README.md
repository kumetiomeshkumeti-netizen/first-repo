well come to the our new journey

Usage
-----

Run the short launcher that executes the example script:

From the repository root:

```
./bin/s
```

Or add the `bin` directory to your PATH to run `s` directly:

```
export PATH="$PWD/bin:$PATH"
s
```

# first-repo
well come to the our new journey 
